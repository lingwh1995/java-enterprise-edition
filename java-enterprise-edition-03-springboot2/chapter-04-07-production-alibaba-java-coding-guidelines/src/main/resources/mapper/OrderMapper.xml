<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.mapper.OrderMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO t_order(order_no, user_id, total_amount, status, shipping_address, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderNo}, #{item.userId}, #{item.totalAmount}, #{item.status}, #{item.shippingAddress}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="updateBatchStatus">
        UPDATE t_order SET status = #{status}, update_time = NOW() WHERE id IN
        <foreach collection="orderIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="deleteBatch" parameterType="java.util.List">
        DELETE FROM t_order WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>