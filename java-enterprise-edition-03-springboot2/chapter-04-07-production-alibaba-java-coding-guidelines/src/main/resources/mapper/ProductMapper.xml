<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bluebridge.dao.mapper.ProductMapper">

    <!-- 插入商品 -->
    <insert id="insertProduct" parameterType="org.bluebridge.entity.ProductDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_product (name, description, price, stock, status)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{status})
    </insert>

    <!-- 批量插入商品 -->
    <insert id="batchInsertProduct">
        INSERT INTO t_product (name, description, price, stock, status)
        VALUES
        <foreach collection="products" item="product" separator=",">
            (#{product.name}, #{product.description}, #{product.price}, #{product.stock}, #{product.status})
        </foreach>
    </insert>

    <!-- 根据ID删除商品(物理删除) -->
    <delete id="deleteProductById" parameterType="java.lang.Long">
        DELETE FROM t_product WHERE id = #{id}
    </delete>

    <!-- 批量删除商品(物理删除) -->
    <delete id="batchDeleteProduct">
        DELETE FROM t_product WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据ID逻辑删除商品 -->
    <update id="logicDeleteProductById" parameterType="java.lang.Long">
        UPDATE t_product SET is_deleted = 1 WHERE id = #{id}
    </update>

    <!-- 批量逻辑删除商品 -->
    <update id="batchLogicDeleteProduct">
        UPDATE t_product SET is_deleted = 1 WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 根据ID更新商品信息 -->
    <update id="updateProduct" parameterType="org.bluebridge.entity.ProductDO">
        UPDATE t_product
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="stock != null">
                stock = #{stock},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 根据ID部分更新商品信息 -->
    <update id="patchProduct" parameterType="org.bluebridge.entity.ProductDO">
        UPDATE t_product
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="stock != null">
                stock = #{stock}
            </if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 根据ID查询商品 -->
    <select id="getProductById" parameterType="java.lang.Long" resultType="org.bluebridge.entity.ProductDO">
        SELECT id, name, description, price, stock, status, create_time, update_time, is_deleted
        FROM t_product
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据名称查询商品 -->
    <select id="listProductByName" parameterType="java.lang.String" resultType="org.bluebridge.entity.ProductDO">
        SELECT id, name, description, price, stock, status, create_time, update_time, is_deleted
        FROM t_product
        WHERE name like CONCAT('%', #{name}, '%') AND is_deleted = 0
    </select>

    <!-- 根据条件查询商品列表 -->
    <select id="listProductByCondition" parameterType="org.bluebridge.entity.ProductDO" resultType="org.bluebridge.entity.ProductDO">
        SELECT id, name, description, price, stock, status, create_time, update_time, is_deleted
        FROM t_product
        WHERE is_deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="price != null">
            AND price = #{price}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <!-- 查询所有未删除商品 -->
    <select id="listProduct"  resultType="org.bluebridge.entity.ProductDO">
        SELECT id, name, description, price, stock, status, create_time, update_time, is_deleted
        FROM t_product
        WHERE is_deleted = 0
    </select>

</mapper>