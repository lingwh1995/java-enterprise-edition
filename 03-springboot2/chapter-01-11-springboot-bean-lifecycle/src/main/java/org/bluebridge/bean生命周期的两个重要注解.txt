1.生命周期中两个重要的注解
    @PostConstruct
    @PreDestroy
2.应用场景
    - 1.初始化时执行一些初始化操作，如连接数据库、加载配置文件等。
    - 2.销毁时执行一些清理操作，如关闭数据库连接、释放资源等。
3.经典用法
    在被@PostConstruct注解标注的方法中
        启动netty服务器
    在被@PreDestroy注解标注的方法中
        关闭netty服务器
