# TXT 笔记格式化规范

> 本规范用于统一格式化 Java 学习教程项目中的 txt 笔记文件。适用范围：模块下所有 `.txt` 笔记文件。
> 使用方法：阅读并理解本规范后，对下一个项目的 txt 笔记文件按此标准进行格式化。

## 1. 文件分类与处理判断

先判断 txt 文件的性质，再决定是否格式化：

| 文件类型 | 判断标准 | 处理方式 |
|---------|---------|---------|
| 笔记 | 内容是知识点整理、需求说明、模式总结、原理讲解等 | 按本规范格式化 |
| 数据/中间产物 | 随机字母、诗词文本、IO 测试文本、SQL 建表/插入语句、编译产物副本（如 target/classes 下） | 跳过，不处理 |
| 空文件 | 0 行 | 跳过，无法判断 |

## 2. 标题规范

- 标题行**顶格**书写，独占一行，不缩进。
- 标题**无编号**、**无冒号**结尾。
- 标题是文件的最高层级，全文只出现一次。

```
✅ 正确
桥接模式

❌ 错误
1.桥接模式
桥接模式:
```

若原文第一行是正文（没有标题），需根据内容**补充**一个恰当的中文标题。

## 3. 条目与层级规范

统一使用「数字 + 点」和「短横线」两种符号表达层级，具体缩进规则如下：

| 层级 | 格式 | 缩进 |
|------|------|------|
| 文件标题 | `标题` | 0 空格（顶格） |
| 一级条目 | `1. ` | 0 空格（顶格） |
| 二级子项 | `   - ` | 3 空格 |
| 三级子项 | `     - ` | 5 空格 |
| 编号子项（同级用 a/b/c） | `   a. ` 或 `     a. ` | 视所在层级缩进 |

```
✅ 正确
设计模式概括
1. 什么是设计模式
   - 设计模式是对软件设计中普遍存在(反复出现)的各种问题，所提出的解决方案
2. 设计模式的目的
   a. 代码重用性
   b. 代码可读性
3. 七大原则核心思想
   - 找出应用中可能需要变化之处，把它们独立出来
```

规则说明：
- 一个层级内若需要编号表达顺序或对应关系，用 `a. b. c.` 而非 `1. 2. 3.`，避免与一级条目 `1.` 混淆。
- 子项缩进必须是**空格**，不能用 Tab。
- 同一文件内层级格式保持一致。

## 4. 代码缩进规范

- 代码块从 5 个空格开始缩进。
- 代码内部保持相对缩进关系，大括号、语句对齐。
- 代码注释使用 `// ` 单行注释，注释文字与代码保持统一缩进。

```
✅ 正确
代码示例:
     public class Runtime {
         private static Runtime currentRuntime = new Runtime();

         public static Runtime getRuntime() {
             return currentRuntime;
         }

         private Runtime() {
         }
     }
```

## 5. 标点符号规范

| 项目 | 要求 | 示例 |
|------|------|------|
| 句中逗号 | 一律中文逗号 `，` | `降低类的复杂度，解除了耦合` |
| 句中分号 | 中文分号 `；` | `先做 A；再做 B` |
| 括号 | 一律半角括号 `()` | `里氏替换原则(最少知道原则)` |
| 引号 | 中文引号 `“ ”` | `“加急消息”这四个字` |
| 冒号 | 中文冒号 `：` 或半角 `:` 均可，但**全文件统一** | 统一使用 `:` |
| 句末句号 | 中文句号 `。` | 用于句末 |

注意：
- 原文中的 Tab 缩进、全角空格、全角括号需统一修正。
- 全角括号 `（）` 改为半角 `()`。

## 6. 单行宽度限制

- 单行最大宽度 **120 字符**，超过需拆分到下一行。
- 拆行时保持当前层级的缩进对齐。

```
❌ 超长行
一个软件实体、函数模块应该对扩展开放(对提供功能的一方而言)，对修改关闭(对使用功能的一方而言)。注意:不是对于客户端开放，客户端是方法的使用方。换而言之，当增加一个新的类或者方法后，原先正在使用的代码不会受到丝毫影响...

✅ 拆分
一个软件实体、函数模块应该对扩展开放(对提供功能的一方而言)，对修改关闭(对使用功能的一方而言)。注意:不是对于客户端开放，客户端是方法的使用方
   - 换而言之，当增加一个新的类或者方法后，原先正在使用的代码不会受到丝毫影响
```

## 7. 错字与术语修正

- 原文中明显的错别字、断字、乱码需结合上下文修正，不能机械照搬原文。
- 常见历史修正示例：

| 原文 | 修正为 |
|------|--------|
| RIM | RMI |
| CGLB | CGLIB |
| JSR-133S | JSR-133 |
| FileOutpStream | FileOutputStream |
| HttpServltWapper | HttpServletRequestWrapper |
| Concreate | Concrete |
| 纬度 | 维度 |
| 出对列 | 出队列 |
| real（队列尾） | rear |
| 面向结构编程 | 面向接口编程 |
| 历史替换原则 | 里氏替换原则 |
| 装换 | 转换 |

- 英文术语与代码中的单词拼写要规范，空格使用半角。
- 口语化冗余表述可精简，但**不改变原意**，不擅自增删知识点内容。

## 8. 处理流程建议

1. 用 Glob 扫描模块下所有 txt 文件，结果超过 200 条时按子目录分批扫描。
2. 逐批读取文件内容，判断是笔记还是数据文件（见第 1 节）。
3. 对笔记文件生成 diff 预览，逐项检查：
   - 是否补充了标题；
   - 层级缩进是否符合第 3 节；
   - 代码缩进是否符合第 4 节；
   - 标点是否符合第 5 节；
   - 行宽是否超 120（第 6 节）；
   - 错字术语是否修正（第 7 节）。
4. 全部检查通过后应用修改。

## 9. 完整示例对照

原文（未格式化）：

```
不使用桥接模式:多个维度耦合再一起，使用桥接模式把不同维度之间的耦合解开
	电脑类型是电脑类型:台式机/平板电脑等
	电脑品牌是电脑品牌:Lenovol/Dell等
使用桥接模式:
	使用组合的模式来代替多层继承，以此解除耦合(多重继承也可以解决此问题，不过Java不支持多重继承，值支持多层继承)
```

格式化后：

```
桥接模式
1. 不使用桥接模式:多个维度耦合在一起，使用桥接模式把不同维度之间的耦合解开
   - 电脑类型是电脑类型:台式机/平板电脑等
   - 电脑品牌是电脑品牌:Lenovol/Dell等
2. 使用桥接模式
   - 使用组合的模式来代替多层继承，以此解除耦合(多重继承也可以解决此问题，不过Java不支持多重继承，只支持多层继承)
```

修正点说明：
- 补充标题「桥接模式」；
- Tab 缩进改为空格层级缩进；
- 「耦合再一起」→「耦合在一起」；
- 「值支持多层继承」→「只支持多层继承」。
