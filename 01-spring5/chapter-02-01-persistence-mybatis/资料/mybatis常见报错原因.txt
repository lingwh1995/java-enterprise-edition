MyBatis 常见报错原因
1. attempted to return null from a method with a primitive return type (int).
   - 标签和方法不对应，如:新增应该使用 insert 标签，结果在 xml 中使用的是非 insert 标签，如使用了 select 标签
2. 必须为元素类型 "insert" 声明属性 "resultType"
   - insert 标签没有 resultType 属性，写代码的时候却使用了这个属性，属于语法错误